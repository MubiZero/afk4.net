# Планы реализации

Здесь лежат только планы незакрытой работы. План уезжает в
`../../archive/superpowers/plans/` в тот же заход, когда его работа влилась в `main`:
папка «активных» планов, где половина уже сделана, врёт о состоянии проекта дороже, чем
экономит времени.

Обновлено 24.09.2026: планы переписывания оболочки P1 «агент говорит правду» (#443) и P2 «сервер
для оболочки» (#444–#448) влиты и уехали в архив. Спека —
`../specs/2026-09-24-player-shell-rewrite-design.md`; P3 (хост) и P4 (интерфейс, #449–#450) —
дальше по ней.

Обновлено 23.09.2026: план редизайна отчётов владельца уехал в архив. Его «хвост» — пояс
филиала, тренд, сравнение периодов, выгрузка из той же проекции — был сделан ещё 29.07
(`4b2f6485`), а строка здесь отставала. Теперь это доказано на настоящем Postgres, и в
выгрузку «Выручки» доехали сравнение и обе разбивки, которые экран показывал, а файл — нет.

Обновлено 16.09.2026: волна честности (PR #264–#275) прошла без новых планов — работа шла
точечными правками по итогам ревизии API и интерфейсов, и её состояние живёт в снимке
прогресса, а не здесь.

Обновлено 03.09.2026: 51 отгруженный план переехал в архив — вся волна платформенной
администрации (август), вся волна приложения игрока, разделение Platform Control и
Organization Admin, перестройка и редизайн панели платформы, релизное управление
обновлениями.

## Активные планы

- `2026-09-24-shell-p4-interface.md` — переписывание Player Shell, план P4 «интерфейс»: каркас на
  теме `player`, экраны по состояниям, вход и выбор времени, сессия, витрина. Вливается вместе с
  хостом P3: они едут одним MSI.
- `2026-06-11-installer-shared-runtime-workstream-a.md` — общий рантайм в установщике
  клиентских приложений. **Заблокирован** решением о production-сертификате подписи: пока
  не назван владелец сертификата и политика хранения ключа, работу начинать нечем.
- `2026-09-25-shell-p3-host.md` — переписывание Player Shell, план P3 «хост и агент»: команды
  машине и обслуживание (P3a), вход игрока через агента (P3b), новый хост (P3c).
- `2026-09-25-shell-p5-kiosk.md` — переписывание Player Shell, план P5 «киоск на Windows»:
  клавиши (P5a), обслуживание по спеке (P5b), учётка и автовход (P5c), профили защиты (P5d),
  стирание следов (P5e), перезапуск службы и тихая установка (P5f).

## Что делать с новым планом

Добавить файл сюда и строку в этот список. Работа влилась — перенести файл в архив и
убрать строку. Если план оказался ненужным, его тоже переносят в архив, дописав в шапке
одну строку о том, почему от него отказались.

## Связанное

- Архитектурный источник правды: `../specs/2026-05-12-afk4-platform-architecture-design.md`
- Индекс спек: `../specs/README.md`
- Операционная дорожная карта: `../../roadmap/production-readiness.md`
- Снимок текущего состояния: `../../progress/2026-05-12-vertical-slice-progress.md`
- Архив отгруженных планов: `../../archive/superpowers/plans/`
