---
name: afk4-multitenant-payments-state
description: "Multi-tenant dcgate payments epic — all subsystems BUILT & MERGED to main; now on a SHARED AFK4 Telegram app (owners give phone+OTP only). Attach proven live on staging; money-path FROZEN by an external bank-bot blocker; prod afk4 deploy deferred."
metadata:
  node_type: memory
  type: project
  originSessionId: e09930db-5386-415e-96b4-9fc396df0c89
---

Each club owner takes player top-up payments on their **own bank card** (possibly a different card per branch). In dcgate **one project = one card**, and dcgate confirms a payment by reading the bank's deposit message in a **Telegram account** tied to that card's bank phone. Payment matching is by a unique `comment` (afk4 `PaymentIntentId`), card is secondary. Repos: afk4 = THIS repo (`AFK4.Platform.Api` + `AFK4.Operator.App.Web`); dcgate = `/home/fedya/projects/dcgate` (MubiZero/dcgate, NestJS+Prisma+gramjs).

**ARCHITECTURE (all 3 subsystems built & merged to main):**
- **A — AFK4 routing core:** `branch_payment_gateways` table (`BranchPaymentGatewayEntity`, BranchId nullable → org-level fallback; ApiKey/WebhookSecret AES-256-GCM encrypted via `ISecretProtector`, key `Secrets:EncryptionKeyBase64`; CardLast4; Status `pending_telegram|active|disabled`). `IBranchPaymentGatewayResolver` (branch-active → org-null-active → null; webhook resolves by `x-dcgate-project-id` header, **fail-closed 401**). Outbound `/api/me/wallet/top-up-intent` → 409 `online_payment_unavailable` if no gateway. Top-up hot-path gate is `Status=active` (live Telegram health shown only in the owner cabinet).
- **B — owner cabinet:** Owner-only perm `payments.gateways.manage`; 6 endpoints under `/api/owner/payment-gateways` (list, provision, telegram start/verify-code/verify-password, status, disable); provision uses externalId=PK + persist-on-first-response, serializable tx for one-active-per-scope (409 `gateway_scope_taken`), 503 if AdminSecret/WebhookUrl unset. Disable frees the scope but keeps the dcgate project (late webhooks still credit). UI: `PaymentGatewaysWorkspace.tsx` (separate, NOT App.tsx monolith), `paymentGateways` client, i18n `payments_cards.*` (ru/en/tg). `IDcGateAdminClient` calls dcgate admin API (`x-admin-secret`, config `DcGate:AdminSecret`/`WebhookUrl`/`PaymentExpiresInMinutes`).
- **C — dcgate hosted Telegram attach:** admin attach state machine `POST /api/admin/projects/{id}/telegram-session/{start|verify-code|verify-password}` behind a `TelegramLoginClient` seam, AES-encrypted session persisted, status `GET /api/admin/projects/{id}/status`. Listener loads sessions at module init → a fresh attach shows `configured` until the worker restarts, then `online`.

**CURRENT DIRECTION — SHARED AFK4 Telegram app (decided + shipped 2026-06-10, PR#70 afk4 `3e1273d` + dcgate PR#5 `dbaae2d`):** abandoned per-owner `api_id`/`api_hash` entry; ONE global `TELEGRAM_API_ID`/`TELEGRAM_API_HASH` lives in **dcgate env**, owners supply only **phone + OTP**. afk4 STOPPED sending creds; the `organization_telegram_api_credentials` table was **DROPPED** (migration `20260610064249_DropOrganizationTelegramApiCredentials`). dcgate attach reads the env creds. (User accepted the Telegram ToS/ban risk of many phones under one app; there is no API to fetch api creds programmatically, so a shared key is the only way to drop owner entry.)

**OPERATIONAL (Coolify `cool.mubi.dev`, host `207.180.237.97`; all API tokens ever used should be REVOKED by the user):**
- **dcgate (C) LIVE in prod** — `https://dcgate.mubi.dev`, Coolify app `tk5e553sv2adamz7p2505a6y`. Real `TELEGRAM_API_ID=27171458` + hash set in the **production** (is_preview:false) env. Prisma migrations auto-apply on deploy. dcgate has **NO GH workflow/secrets** → redeploy/worker-restart needs a Coolify personal token or the UI.
- **afk4 (A+B) LIVE on staging** — `https://afk4.staging.mubi.dev`, app `d3fm17hl6kb7sossg1kj8buq`. Env set: `DcGate__BaseUrl`, `Secrets__EncryptionKeyBase64` (32-byte key — user holds the ONLY off-Coolify copy; losing it = stored gateway creds undecryptable), `DcGate__AdminSecret` (= dcgate `ADMIN_JWT_SECRET`), `DcGate__WebhookUrl`, `DcGate__PaymentExpiresInMinutes`. afk4 does **NOT** auto-migrate; staging DB `afk4-staging-postgres` (uuid `foie51tkp2w68t6nwc5xks63`, public_port 55432) migrated through HEAD incl. the DropTable. **Redeploy afk4 staging without a personal token:** `gh workflow run coolify-staging-deploy.yml --repo MubiZero/afk4.net --ref main -f confirm_migrations_applied=true` (uses the GH-stored `COOLIFY_API_TOKEN`; the workflow gates on the confirm flag for any `Data/Migrations/**` change). Manual dispatch only — no auto-deploy. Full runbook in [[afk4-env-quirks]].
- The owner cabinet UI lives in the **desktop** Operator.App (WebView2), not a staging web app → staging e2e is API/curl-driven.

**STATE — attach PROVEN live, money-step FROZEN (external blocker):** On staging, phone-only `start` → dcgate used the shared env creds → OTP → `attached`; worker restart → session `online`; **session-sharing confirmed** (attaching the SAME phone to a 2nd project linked instantly, no 2nd OTP). **BLOCKER:** the DC bank bot stopped delivering deposit notifications to new clients, so the real payment → webhook → wallet-credit step can't be exercised. User froze payments until the bot recovers. **Nothing in our code is implicated.**
- e2e fixtures (to resume): org `0169044b-2f74-46a7-8e52-7656a39a8f8c`, owner `e2eowner`/`E2eOwner!2026` (has `payments.gateways.manage`), player `+992900000001`/`112233`. Active gateway = card 5500 (`9762000085435500`), gateway `d546320b-a934-41a5-b832-f12132c0d134`, dcgate project `cmq7tksva0002ll010lrlpssi`, org-level; attached phone `+992920211467`. Old card-1953 gateway `93eda272` was disabled. Pending intent `d22e654d` (1 TJS) left to expire.

**STILL TODO:** (1) when the bank bot recovers, re-run the money e2e end-to-end (phone+OTP → online → top-up → webhook → wallet credited). (2) **Prod afk4 deploy** still deferred — Coolify app EXISTS but is EMPTY (project `sm54z5to8ibzt4u4jd5jt1qz` / env `v3x8bitios8nrdfxllp2d0y4`); repo has only `staging.*` deploy config, so prod = build-from-scratch (full env incl. the encryption key + DB + migrations). Backlog: live listener start for a brand-new account without a worker restart.

Origin: customer-shell Unit 2 online top-up — see [[afk4-sp4-shipped]]. Env/runbook in [[afk4-env-quirks]].
