---
name: operator-redesign-phase0-decisions
description: "Operator App full-overhaul epic — program decomposition (Этапы) + all LOCKED foundation (Этап 0) decisions; spec WRITTEN. Resume point for the redesign."
metadata: 
  node_type: memory
  type: project
  originSessionId: 8ca9965a-38e4-43de-a075-003f1af1b3c8
---

Эпик **полной переработки Operator App** (НЕ только UI — лейауты + переосмысление всех экранов + функциональность). Открыт 2026-06-14. Разведка конкурентов закрыта → [[operator-competitor-ux-teardown]]. Текущая IA → [[operator-rail-sections]].

**ТЕРМИНОЛОГИЯ:** говорим **«Этап»** (не «фаза»/«слайс» — унифицировано 2026-06-15). Этап 0 = сквозной Фундамент; Этапы 1–6 = по одной области целиком, каждый свой `спек → план → PR`.

**ПРОГРАММА (этапы по порядку):** **0 Фундамент → 1 Карта → 2 Касса → 3 Клиенты → 4 Брони → 5 Отчёты → 6 Управление.** Глубину функциональности каждого этапа решаем в ЕГО брейншторме.

**СТАТУС:** спек Этапа 0 написан (`docs/superpowers/specs/2026-06-14-operator-foundation-design.md`). Решено делать Этап 0 **по кускам** (отдельный план→PR на каждое из 5 направлений §9). **Кусок «Деньги» РЕАЛИЗОВАН и смерджен в main** (ff, `bbf9000`; план `docs/superpowers/plans/2026-06-15-operator-foundation-money.md`; локально, НЕ запушен). Гейты: Operator 217/217, money 4/4, build 0. **Кусок «Видимость роль→секции» РЕАЛИЗОВАН** (ветка `feature/operator-role-visibility-contract`, план `docs/superpowers/plans/2026-06-15-operator-foundation-role-visibility.md`): guard-тест `operatorVisibility.test.ts` (фронт) + `PermissionCatalogContractTests.cs` (бэкенд). Сверка с `PermissionCatalog.cs` дала расхождения, устранены: `shift_supervisor` +`billing.money_action.approve` −`audit.view`/`diagnostics.view` (Касса с одобрением, без Управления); `accountant_auditor` оставлен read к Клиентам/Броням (поправлена таблица §2); протечки секций от пассивных view-прав убраны ужатием `workspacePermissionRules` (секция зажигается от *действий/manage*). Гейты: Operator 222/222 + build, Platform.Api 1159/1159. **Кусок «Примитивы» РЕАЛИЗОВАН** (ветка `feature/operator-foundation-primitives`, спек `…-primitives-design.md` + план `…-foundation-primitives.md`): Toast net-new (`operatorToast.tsx`: ToastProvider+useToast+viewport, 3 тона success/error/info без warning, очередь≤3, error липкий/остальные авто-4с, action в API заложен) подключён в корень App; Skeleton+EmptyState извлечены в `operatorPrimitives.tsx`; `useDeferredFlag` уже был — не трогали; reduced-motion гасится глобальным CSS. Догфуд: Map/Dashboard skeleton, Map/ShopOrders empty, живой success-toast на действиях ShopOrders (accept/deliver/cancel). i18n ru/en/tg (реальный tg). Гейты: Operator 237/237 + build. **Кусок «shell-каркас» РЕАЛИЗОВАН и СМЕРДЖЕН** (PR #87 → `45d8af7`, ветка удалена; спек `…-shell-design.md` + план `…-foundation-shell.md`): `QuickActionsMenu` (8 действий «+», фильтр прав, клавиатура, хоткей-подсказки, дис-патч через `createCommandRegistry` в `operatorCommands.ts` → пустой реестр даёт honest-toast `op.command.deferred`; этапы регистрируют обработчики); `CommandPalette` (⌘K реальная навигация по `navSections`+права, focus-trap/Esc, ARIA combobox+listbox, поиск сущностей помечен «скоро»); `useHotkeys` (глоб. keydown, игнор repeat/инпутов, cleanup); `ContextPanel` (shell-сворот правой зоны + персист `afk4.operator.contextCollapsed`, нет контента→`--shell-context-col:0`; НЕ дублирует `.context-panel` MapSidePanel — отдельная `.context-zone` с колонкой-ручкой); `ShellAlerts` (danger-тон при problems>0, шов `sources` под Карту). Удалён `SummarySidePanel` (закрыт техдолг). Мёртвый ключ `op.shell.signals` вычищен. Гейты в изоляции: Operator 264 + tsc + build. **ЭТАП 0 «ФУНДАМЕНТ» ПОЛНОСТЬЮ ЗАКРЫТ.** **Этап 1 «Карта» — брейншторм ЗАКРЫТ, спек написан** (`docs/superpowers/specs/2026-06-15-operator-map-design.md`). Объём = ПОЛНЫЙ: командный центр (A) + редактор плана зала (B, incl. бэкенд-модель пола). Декомпозиция на 5 кусков, каждый → свой план→PR, порядок **A1→A2→A3→B1→B2**: **A1** язык плиток (канон статусов, предоплата=остаток+бар / постоплата=сумма «54 с. ▲» / свободно=«＋»); **A2** ПКМ контекст-меню (живые действия + честные «скоро»-заглушки через `op.command.deferred`) + строка критсостояний со счётчиками (шов `sources` у `ShellAlerts`, клик→фильтр); **A3** полировка `MapSidePanel` (конкретика #34, точные ошибки); **B1** пространственная модель пола — `SeatEntity`+`PosX/PosY/Rotation/SeatType`, `ZoneEntity`+`GeoX/Y/W/H/Color/ZoneType`, новая `WallEntity(отрезок)`, расширяем `FloorMapBulkUpdateRequest`+`EfFloorMapEditService`+DTO+миграция (всё nullable/аддитивно); **B2** вид «План»-редактор (холст·снап·палитра·инспектор) + «Сетка» рисует по координатам с фолбэком на поток по `SortOrder`. **УТОЧНЕНИЕ к Этапу 0:** виды = **Сетка·Таблица** (Таблица — список-данные, сохраняем), а «План» — отдельный режим-редактор, НЕ третий вид (старая фиксация «Сетка·План без Оба» уточнена). **Бэкенд-факт (проверено):** пространственной модели НЕТ, только `SortOrder`; edit-сервис `EfFloorMapEditService` уже есть → B1 его расширяет. Стартуем с A1, ветка `feature/operator-map-tiles`. **RESUME POINT** (2026-06-15). См. [[operator-feedback-fewer-questions]] — в активной фазе не спрашивать очерёдность.

**ДОЛГ:** ~~`SummarySidePanel.tsx`~~ ЗАКРЫТ (PR #87). ~~флак-тест App.test~~ **ПОЧИНЕН (PR #88 `354ed02`).** Был: `App > opens the loyalty settings workspace` мигал ~50-80% ТОЛЬКО при полном `bun test` (унаследован с main, НЕ от каркаса). Корень (доказан): bun держит `mock.module` на весь процесс + недетерм. порядок файлов; соседи (`ShopOrders`/`PaymentGateways`/`PhoneVerification`/`posAvailableInShell`/`ForgotPassword`) глобально мокают `operatorApiClients`/`operatorHelpers`/`operatorRealtime`/`authClient`, а `App.test` (единств. широкий интеграционный) хочет РЕАЛЬНЫЕ клиенты + СВОЙ realtime-мок → чужие моки перебивают, данные не грузятся, заголовок «AFK4 Dushanbe» не рендерится. НЕ тайминг (20s таймаут не спасает). Изоляция доказана: App.test один 88/88, прочие 34 файла вместе 176/176, флак только при со-резидентности. **Фикс:** `package.json` "test" гоняет App.test ОТДЕЛЬНЫМ `bun test`-вызовом (изоляция процесса), без рефактора mock-файлов; причина в `src/test/setup.ts`. **НЕ сливать App.test обратно в общий прогон** — флак вернётся. Victim-side переустановка моков в beforeEach НЕ помогала/ухудшала.

**LOCKED-решения Этапа 0:**
- **Оболочка = вариант B «Evolve».** 3-зонный shell (рейл·рабочая·контекст) + 6-секционная IA. ДОБАВИТЬ: верх-полоса «+ Быстрое меню», поиск/палитра ⌘K, хоткеи, сворачиваемая контекст-панель, строка тревог в статус-баре. (Вариант C отвергнут.)
- **Командный слой:** «+» = 8 действий не привязанных к объекту (Запустить сессию/Продажа/Создать клиента/Новая бронь/Пополнить депозит/Приходный/Расходный ордер/Внесение на склад; смена НЕ тут — в профиле). ⌘K палитра = поиск сущности+переход+действие. Привязанные к ПК действия — на месте (клик/ПКМ), не в «+».
- **Визуальный язык места:** статус = спокойная база + полоса слева, громкий цвет только Внимание/Проблема; канон статусов (играют/внимание/проблема/свободно/бронь/обслуживание/выключен, не только цветом). Плитка = ОДИН размер, **density НЕТ**; предоплата=остаток времени внизу+бар, **постоплата=сумма вверх «54 с. ▲»**; свободно=«＋». Большой зал=скролл.
- **План зала:** виды **Сетка·План** (без «Оба»). План=холст сетка-снап, полная палитра (хосты/структура/зоны)+инспектор. Модель пола: место={x,y·тип·поворот·привязка·зона}, зона={прямоуг·тип·имя·цвет}, стена=отрезок. UI-редактор — в Этапе «Карта».

**РЕШЕНО в этой сессии (бывшие открытые вопросы):**
- **Роле-видимость = эталон-контракт «роль→секции» + guard-тест.** Механизм НЕ трогаем — навигация уже permission-driven (`canOpenWorkspace` по `session.permissions`, `operatorPermissions.ts`). 5 ролей: cashier_operator/shift_supervisor/branch_manager/technician/accountant_auditor. Таблица видимости в спеке §2 — ЦЕЛЕВОЙ контракт, сверить с бэкенд-маппингом прав (не подгонять под текущее, #37).
- **Дизайн-система = канон существующего, не изобретать.** Токены в `@afk4/tokens` уже сильные: типошкала 7 ступеней (11/13/14/16/20/26/32), motion (`--duration-fast 120`/`medium 200`/`modal 320` + ease-out/in/spring), focus-rings, **light в паритете с dark**. Примитивы: слой тонкий (`operatorPrimitives.tsx` = 3 шт; кнопки/таблицы/формы = CSS-классы в `styles.css` ~6500 строк). Решение: **«канон сейчас, извлечение по ходу»** (НЕ big-bang). Завести недостающие **Toast/Skeleton/EmptyState**. Motion-паттерны: feedback<100мс, optimistic (на offline-outbox), отложенный спиннер 150–300мс, скелетоны с финальной геометрией, prefers-reduced-motion.
- **Валюта = единый форматтер + знак (РЕАЛИЗОВАНО, в main).** Знак валюты — const-мапа `currencySymbol()` в `@afk4/money` (НЕ i18n-json: иначе пришлось бы тащить `t()` в десятки вызовов; знак НЕ язык-зависимый, TJS→«**с.**», USD→«$», фолбэк=код). Форматтер `formatMinorUnits` вынесен в листовой `src/AFK4.Operator.App.Web/src/currencyFormat.ts` (избегает цикла operatorHelpers↔floorMapState), реэкспортится из operatorHelpers; `formatMoney(null)`→ноль. Все money-surfaces (плитка/касса/платежи/клиенты/смены/ShopOrders/MapSidePanel) переведены на него, ISO-код «TJS» из UI убран. Десятичные: целое→0 знаков, дробное→до 2, ru-RU. Известный нюанс (вне скоупа): числовая локаль захардкожена `ru-RU`.

**ПРОЦЕССНЫЙ УРОК:** НЕ доводить дизайн попиксельно в HTML-мокапах (зарылись в «Карту» = ложный регресс). Решения — в спек, тонкую визуалику — в код этапа. Мокапов 1–2 на решение, не больше.

**Код (старт):** `src/AFK4.Operator.App.Web/src` (React 19 + vanilla CSS `styles.css` ~6500 строк). Навигация — `navSections` (`operatorData.ts`); права — `operatorPermissions.ts`; токены — `@afk4/tokens`; i18n — `@afk4/i18n` ICU; деньги — `@afk4/money`+`formatMinorUnits`; примитивы — `operatorPrimitives.tsx`; воркспейсы — `MapWorkspace.tsx` и др.
