---
name: afk4-productionize-installer-epic
description: "Productionize-installer epic CLOSED — B (prod URL) MERGED PR #72; A (shared runtime + branding + zero-click) VM-VERIFIED & MERGED PR #74; C (signing) WON'T-DO (dropped)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 06f4ec90-ea92-4239-88c1-ca8a361777b9
---

Next installer epic, **specced but not started** (scope to resume in a new session).
Design + phased plan: `docs/superpowers/specs/2026-06-11-productionize-client-installer-design.md` (on main, listed as active in that dir's README).

Three workstreams:
- **A — size+UX: ✅ VM-VERIFIED & MERGED PR #74** (`e6008fc` on main; branch deleted; orig branch was `feature/installer-shared-runtime`; plan `docs/superpowers/plans/2026-06-11-installer-shared-runtime-workstream-a.md`). Four components now publish **framework-dependent**; ONE carried **.NET 10 Desktop Runtime** (10.0.9) embedded in a **WiX Burn bundle** chaining: Desktop Runtime (detect-skip) → agent MSI. Deliverable is `afk4-client-<ver>-<channel>.exe` (**65 MB**, down from the 168 MB self-contained agent MSI = ~61%); the agent/operator/player-shell MSIs move to `intermediates/`. **The branch as first pushed could NOT build** — I fixed 4 WiX-7 issues (commit `0a8d4bc`): `wix extension add` needs `-acceptEula wix7` (OSMF EULA WIX7015); `WixToolset.Bal.wixext`→`WixToolset.BootstrapperApplications.wixext` (renamed in v7, WIX0144; `bal:` namespace unchanged); Bundle.wxs ExePackage can't have `Compressed`+`ExePackagePayload` (WIX0372) nor payload `ProductName/Description` w/o Hash (WIX0037); `Get-MsiFileNames` leaked WindowsInstaller COM handles → blocked the agent-MSI `Move-Item` (release COM + GC). Plus commit `cec0bf4`: wizard didn't auto-launch under the bundle (Burn runs MSIs at UILevel=none(2), failing the `UILevel>=3` gate) → bundle passes `LAUNCHWIZARD=1`, agent MSI condition ORs it in (headless `/qn` still silent). **Verified 2026-06-12 on a clean Win11 snapshot:** runtime absent→10.0.9 present, agent service starts Running (no sc-1053 — the old revert's failure), wizard auto-launches, staging enrollment completes, FD Operator App installs-from-payload and runs. Build it from WSL: `pwsh -File D:\projects\afk4.net\run-build.ps1` (store pwsh7 `…\WindowsApps\pwsh.exe`, bun.exe `C:\Users\Mukhamedov.M\.bun\bin\bun.exe`) — see [[afk4-env-quirks]] D:\ clone. **2026-06-12 polish (VM-verified, still 65MB, commit `c42d1e2`, in PR #74):** (1) **branding** — bundle `IconSourceFile`=`brand/dist/afk4.ico` (exe icon) + WixStdBA `LogoFile`=`brand/dist/icon-64.png` (window logo); assets git-tracked, generated by `scripts/build-brand-assets.mjs` from `brand/afk4-icon.svg`. (2) **zero-click** — a tiny **native C++ BAFunctions DLL** (`installers/bundle/bafunctions/`, `AFK4.BAFunctions.dll`) plugs into WixStdBA: `OnThemeControlLoaded` grabs the `"InstallButton"` HWND, `OnDetectComplete` (full-display only) `PostMessage(BM_CLICK)` it (auto-start install — same path a manual click takes), `OnApplyComplete` (success+no-reboot) `PostMessage(WM_CLOSE)` (auto-close); UAC stays (per-machine elevation; only signing tames it). Built via **MSBuild** (needs VS C++ toolset — user installed VS2026 toolset `v145`; build script finds MSBuild via vswhere), refs NuGet `WixToolset.BootstrapperApplicationApi`+`WixToolset.DUtil` 7.0.0 for headers/`balutil.lib`/`dutil.lib`, **vendors** the BAFunctions SDK headers+`BalBaseBAFunctionsProc.cpp` from WiX **v7.0.0** under `bafunctions/wixsdk/` (re-vendor if the wix tool bumps). MUST stay native + `/MT` (runs before .NET/VC++ exist on the box). Wired via `<Payload … bal:BAFunctions="yes">`; verify embed with `wix burn extract -acceptEula wix7` (look for `AFK4.BAFunctions.dll` + `<WixBalBAFunctions FilePath=…>`). Bundle build now also needs `-d BAFunctionsPath/BrandIconPath/BrandLogoPath`. Tests: `ClientReleaseAutomationTests` got 2 new text-parse tests (green on Linux).
- **B — prod URL: ✅ DONE & MERGED — PR #72** (also merged PR #73 removing the dead legacy WPF wizard `MainWindow`/`SetupWizardViewModel`, which carried the last stray hardcoded staging URL; and fixed two pre-existing main CI reds on the way: a stale player-shell build-script test + unconfigured `AgentUpdateWorkerTests` — see [[afk4-env-quirks]]). (branch `feature/prod-url-by-channel`, plan `docs/superpowers/plans/2026-06-11-prod-url-by-channel.md`). `SetupWizardDefaults.PlatformBaseUrl` now resolves from `[AssemblyMetadata("AFK4.PlatformBaseUrl")]` (validated absolute http(s), staging fallback); csproj emits it on `-p:AFK4PlatformBaseUrl`; build script maps `-Channel` (`internal/beta`→staging, `stable`→**`https://app.afk4.net`**). **Prod domain decided: `app.afk4.net`** (landing stays on root `afk4.net`); **prod NOT deployed yet** — only plumbing baked, no installer change needed once it goes live.
- **C — signing: ❌ WON'T-DO (dropped by user 2026-06-12).** MSIs/bundle stay unsigned → SmartScreen + "unknown publisher" UAC are accepted. Do NOT re-propose signing unless the user revives it.

**CI follow-up (2026-06-16, PR #93 merged):** A's bundle change (master `afk4-client-<ver>.exe` + per-app MSIs moved to `client-packages/intermediates/`) had left the **Package Smoke** workflow + `publish-client-msi-updates.ps1` still looking for the per-app MSIs at the package ROOT → `Package Smoke` was RED on every push to main since A landed (verify step "Expected MSI artifact was not produced"). Fixed by realigning to the bundle layout: `package-smoke.yml` verify now checks the bundle `.exe` + `intermediates/*.msi` and uploads the new layout; `publish-client-msi-updates.ps1` resolves the 3 per-app MSIs from `intermediates/` (fallback to root); `client-packages.yml` upload glob updated. **Verified green** via `workflow_dispatch` on the branch AND post-merge on main. (Package Smoke runs only on `push: main` w/ path-filters + `workflow_dispatch` — NOT on PRs, so validate a fix by dispatching it on the branch before merge.) Signing steps left untouched (C dropped).

Order was **B → A → C**: B + A both MERGED; **C dropped. Epic CLOSED (#74 merged 2026-06-12).** Note: A's CI was unblocked by fixing an unrelated date-bomb in `DeviceCommandEndpointTests` (hardcoded enrollment-code `ExpiresAtUtc` that expired on 2026-06-12; now relative to `UtcNow`) — see [[afk4-env-quirks]].

Relates to [[afk4-client-demo-runbook]] (the shipped one-MSI/all-roles base this builds on).
