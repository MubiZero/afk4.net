---
name: afk4-operator-map-epic
description: "Operator App «Карта» (Stage 1) redesign — piece status, branches/PRs, key decisions"
metadata: 
  node_type: memory
  type: project
  originSessionId: fde4c0e8-7836-49c1-85e3-7b8af69217f9
---

Этап 1 «Карта» Operator App: превратить экран мест из «таблицы статусов» в командный центр.
Спека: `docs/superpowers/specs/2026-06-15-operator-map-design.md`. Декомпозиция A1→A2→A3→B1→B2.

> **⚠️ РАЗВОРОТ ПО «ПЛАНУ» (2026-06-17): вид «План» УДАЛЁН, карта = ТОЛЬКО ГРИД.** Владелец сначала откатил переключатель (commit `6072a61` «убрать вид „План", оставить грид»), затем поручил **удалить весь фронтовый код «Плана»**. Сделано в этой сессии: снесён остров из 20 файлов (`FloorPlan*`, `PlanSeat`, `FloorPalette`, `FloorZone*`, `FloorInspector`, `floorPlan{Draft,Geometry,State}` + тесты); вырезан мёртвый сейв-путь (`handleSaveLayout` в `useFloorMap`, `updateFloorMap`+bulk-write типы в клиенте `floorMap.ts`); из 3 локалей убраны 52 ключа (`op.map.plan.*` ×48 + `viewGrid/viewPlan/viewLabel/nextLayer`), `messages.ts` регенерирован, allow-list в `messages.test.ts` подчищен; из `styles.css` снесено 456 строк план-CSS. **Что СОХРАНЕНО осознанно:** GET-слой floor-map (`getFloorMap{,WithEtag}`) — его использует грид; generic `platformApi` (get/put/getWithEtag) как транспорт; read-DTO с гео/walls-полями как контракт бэка; `op.shell.err.mapNotLoaded` (живой, стр.217). **Бэкенд НЕ тронут** (endpoint PUT `/floor-map`, `WallEntity`, гео-колонки, миграция `AddFloorPlanGeometry`) — это отдельное решение, мёртвый write-endpoint висит как бэк-техдолг, поднять отдельной задачей если решат чистить БД. Проверки зелёные: i18n 34/0, operator 227+88/0, build (tsc+vite) OK. **Удаление СМЕРЖЕНО В main и ЗАПУШЕНО (commit `cc72a37`, fast-forward; вместе с ним уехал и не запушенный ранее `6072a61`).** Хвосты подчищены ПОЛНОСТЬЮ: ветки `feature/operator-plan-konva-editor` уже не было; залежавшийся агентский worktree `worktree-agent-a3fc3c7817e2ba481` (старый План `9132cb8`) удалён; Konva-коммит `cfa828f` физически вычищен (его держали ORIG_HEAD + reflog + **stash** `phase1-smartshell-wip` с План/Konva WIP — всё срезано, `gc --prune=now`). Заначек/висячих коммитов нет. NB: `floor.*` i18n-ключи и `AFK4.Platform.Web/.../FloorMapEditor` — это ДРУГОЙ (venue) редактор, живой, не путать с операторским «Планом».

**ВСЁ A1–A3 + B1-данные СМЕРЖЕНО В main (2026-06-16, PR #92 — один мерж всего эпика).** #89 GitHub отметил merged, #90/#91 закрыты, все ветки стека удалены. Footgun-миграция `AddSessionBillingMode` **применена к staging-БД вручную + редеплой** (health ok) — рунбук в [[afk4-env-quirks]]. **ЭПИК «КАРТА» (Этап 1) ЗАКРЫТ ЦЕЛИКОМ (2026-06-16).** Всё A1–A3 + B1 + B2 (B2-1 бэкенд, B2-2 вид «План», B2-3a расстановка мест, B2-3b зоны, B2-3c стены) смержено в main (PR #92, #94–98). Открытого скоупа по эпику нет — следующий значимый кусок брать на уровне ПРОЕКТА (не углубляться в карту).

Статус (история, на 2026-06-15 — все в main через #92):
- **A1 — язык плиток** (статус-цвет, предоплата/постоплата/свободно-«＋», убраны fake billing/тех-данные): DONE (был PR #89).
- **A2 — ПКМ контекст-меню + строка критсостояний**: DONE (был PR #90).
- **A3 — полировка + редизайн правой панели** (`MapSidePanel`): DONE (был PR #91). Убран фейковый биллинг-режим (хардкод Wallet, #37); честные деньги. Затем полный редизайн в «командную карточку» (герой остаток+полоса, доминирующая CTA «Завершить и оплатить», действия-стек, здоровье ПК пилюлями) + консолидация: диагностика свёрнута до заголовка+пилюль, команда/версии/подтверждение показываются только когда actionable (attention/pending/нет прав/нет биллинга), дубль «Устройство» убран, плейсхолдер клиента убран до B1.
  - Финал A3 (2026-06-16): старт/расчёт открываются ЦЕНТРИРОВАННЫМ модалом (`PanelModal`, портал в body) — в узкой панели формы тесные; «Стоп»+«завершить и оплатить» слиты в одну «Завершить сессию» (онлайн→расчёт с «без оплаты», офлайн→подтверждение); пофикшен серый экран (CheckoutDialog читал `quote.grandTotal` без optional-chain, а панель ВНЕ WorkspaceErrorBoundary→краш гасит весь app). Затем: герой не показывает гигант «Свободно» для free-места; «Посадить гостя» на плитках крупнее; подтверждение действия СТАЛО ВИЗУАЛЬНЫМ (спиннер на кнопке→зелёная галочка `ActionFeedback`, авто-гаснет; ошибка остаётся текстом), строка готовности больше не эхо-ит действие.
  - Полировка оболочки (2026-06-16, commit `c5ec7be`): «Выйти» убран из шапки → в подвал левого рейла (`.rail-logout`, `margin-top:auto`, danger-hover). Футер `.signals-strip` приведён к единому стандарту: каждый элемент — `.signal-item` (иконка/точка + текст, один размер/вес), связь показывает цветную точку-severity `.signal-dot ok/warn/bad`, источник данных — `.signal-muted`, правый кластер `.signals-right` (касса + lock-feedback) прижат `margin-left:auto`; слева система, справа бизнес-метрика. Шапка+лого (commit `cd80fe2`): лого-SVG имел viewBox 250 при глифе ~190 → ~36px «воздуха» справа = большой гэп до разделителя; обрезан до viewBox 206 (НЕ туже: SVG в `<img>` рендерится изолированно, веб-Inter недоступен → Segoe-фоллбэк шире, 196 рисковал клипнуть «T»), `.brand-block` gap 12→8 → лого→разделитель ≈ разделитель→«Оператор». Попап «Офлайн: ждём ответ сервера» на карте был легаси-баг: `FeedbackNotice` со `state:'pending'` показывал генерик «{label}: ждём ответ сервера», ВЫБРАСЫВАЯ реальный `op.floor.offlineBanner` (со временем кэша), и врал «офлайн» при живой связи. Чинено: `offlineBannerText` теперь различает по `isOffline` (реальный обрыв → `op.floor.offlineBanner` «только просмотр»; устаревший снимок при связи → новый `op.floor.staleBanner` «Снимок зала на HH:MM, обновляем…»); рендер — отдельный `.floor-snapshot-banner` (WifiOff/Clock иконка, тон offline=warning / stale=нейтр), не pending-спиннер. Шапка карты: убран избыточный надзаголовок «Карта» (`op.map.title`, секция ясна из навигации), фильтры+переключатель вида `.map-filter-row`/`.map-view-switch` переделаны из россыпи пилюль в единый сегмент-контрол (один трек, активный сегмент залит, счётчик — бейдж). Доводка (commit `fba391a`): заголовок врал «AFK4 Dushanbe · зал A», хотя карта показывает все залы — суффикс «· зал A» был зашит в демо-данные (`devMockBackend` + `fixtureBranchName`), убран → просто «AFK4 Dushanbe» (real-бэк отдаёт своё имя; тесты на `/AFK4 Dushanbe/` частичные, ок). Кнопка сворачивания правой панели (`ContextPanel`): из боковой 22px-колонки-жёлоба перенесена в тонкую шапку НАД панелью, прижата вправо (как «collapse» у IDE) — `.context-zone` грид сменён с 2 колонок на 2 строки (auto / minmax(0,1fr)), body-скролл сохранён; кнопка осталась ghost. **Откат+упрощение (commit `d2625bb`):** кнопка сворачивания «не прижилась» — УБРАНА совсем (`ContextPanel` теперь просто слот, без тоггла/collapsed/handle; в App убраны `contextCollapsed`/`toggleContextCollapsed`, `contextCol` всегда expanded; панель и так видна только при выбранном месте). Баннер «снимок зала на HH:MM, обновляем…» (stale при живой связи) убран как тех-шум — `offlineBannerText` теперь возвращает баннер ТОЛЬКО при настоящем `isOffline` (D8-логика «устарело >30с» удалена вместе со `staleAfterMs` и ключом `op.floor.staleBanner`); остался только важный офлайн-баннер «только просмотр». В превью баннер висел вечно, т.к. демо-данные заморожены (нет живого сетевого refresh). **PC-control в панель (commit `3ad745d`):** отдельностоящая тулбар-кнопка «Управление ПК» + плавающий попап (`.pc-control-panel`) убраны; PC-команды (Статус/Заблокировать/Разблокировать) интегрированы как секция «Управление ПК» в карточку выбранного места (`MapSidePanel`), рядом со «Статусом ПК» — видишь состояние машины и тут же командуешь. Своё `pcFeedback`-состояние (спиннер→галочка на кнопке как у действий сессии), а результат «Статус» показывается ТЕКСТОМ (`.pc-control-result`) — это отчёт (версии/состояние), его надо читать (#34), а не гасить «Готово». Future-команды (reboot/shutdown/wake/admin) НЕ дублированы в панель — они и так живут в ПКМ-контекст-меню (`buildSeatMenu` kind:'soon'). Пропсы `canUsePcControl`/`onPcControlAction` теперь у MapSidePanel (не MapWorkspace); `runPcControlAction`/`explainUnavailablePcControl`/`feedback` в MapWorkspace ОСТАЛИСЬ (их использует контекст-меню). Мёртвый CSS попапа/тулбар-кнопки вычищен. Доводки по фидбэку владельца: панель опущена на уровень сетки мест (`.context-zone` padding-top 72px = высота шапки карты); «Статус ПК» оставлен НАД «Управление ПК» (пробовали наверх панели и блок «Скоро» reboot/shutdown/wake — владелец откатил, убрано); кнопка «Статус» показывает отчёт текстом (`.pc-control-result`) — в ЛОКАЛЬНОМ превью отчёт выходил «Агент unknown», т.к. `devMockBackend.route()` не знал `/devices/{id}` и `/diagnostics` → добавлены фикстуры `deviceDetail()`/`diagnostics()` (commits `4cfb0be`+`bd60b61`). Тревоги в футере (`ShellAlerts`, commit `8a39928`): убран дубль — был агрегат `op.alerts.summary` «N проблем · M офлайн» И тут же разбивка чипами, где «M офлайн» == чип «Нет связи M». Теперь чистая модель: ЛИБО разбивка критических по типам (Нет связи/Сбой/Обслуживание, кликабельны), ЛИБО позитив `op.alerts.allClear` «Всё в норме». Пропсы problems/offline убраны. realtime-строки `op.helper.realtime.*` переименованы «Связь»→«Сеть» (Network / Шабака) — владелец просил «сеть подключена». Карточка-заголовок панели: зона+имя слиты в `.seat-hero-head`>`.seat-hero-id` (надзаголовок вплотную над именем, чип статуса справа) — было ощущение «заголовок маленький, подзаголовок большой». Кнопка сворачивания правой панели (`.context-zone-toggle`) сделана ghost (без рамки) и прижата к панели (`.context-zone-handle` justify flex-end, padding-top 10px = panel margin-top) — была «косо в жёлобе». Левый рейл `.workspace-rail` получил явный `overflow-x:hidden` — убран фантомный горизонтальный скролл (overflow-y:auto иначе коэрсит overflow-x в auto). Ключ `op.map.panel.diagnostics` переименован «Диагностика ПК»→«Статус ПК» (PC status / Ҳолати компютер).
  - **Типографика (соглашение на ВЕСЬ operator `styles.css`):** веса — `500` инпуты/вводимые значения · `600` лейблы/кнопки/чипы/подписи · `700` заголовки/секции/strong/имена/primary CTA · `800` ТОЛЬКО крупные числа-герои (остаток/таймер/часы/числа чартов). `900/850/750/300` не использовать. Размеры — шкала `11·12·13·14·16·18·20·24·28` (плотное тело 10–13 ок). Был аудит (2026-06-16): до него 87 объявлений ≥750 — «всё кричало». Не возвращай вес 800 на мелкие лейблы.
  - **Соглашение по терминологии (действует на ВЕСЬ operator UI):** слово «Платформа» в копи оператора = жаргон, заменено на «сервер» / формулировку результатом (~69 строк ru/en/tg). Будущие строки писать так же. ВНИМАНИЕ к роду в ru: «сервер» муж. → «подключён/не подтвердил/недоступен» (не «подключена»). Админ-консоль (`platform.*`, `auth.admin.*`, `nav.platform.*`) НЕ трогаем — там платформа = продукт.
  - **Footgun продления — ЗАКРЫТ (2026-06-16, ветка `feature/operator-map-data`):** выбран подход B. `SessionEntity.BillingMode` (varchar(32), миграция `AddSessionBillingMode`, default '' для легаси-строк) проставляется при старте (`billingMode` уже нормализован стр.71); `ExtendSessionAsync` считает `effectiveBillingMode = пустой request.BillingMode ? session.BillingMode : request.BillingMode` и шлёт его и в `ValidateExtendAsync`, и в `AppendExtendLedgerEntriesAsync` — пустое продление больше не падает в бесплатного гостя для платной сессии (явный режим в запросе по-прежнему побеждает = комп оператора). Тесты: старт сохраняет режим + продление наследует (fake фиксирует `LastExtendBillingMode`). **Деплой-нюанс:** Coolify-workflow блокирует коммиты, трогающие `Data/Migrations/**`, пока миграцию не применят вручную и не вызовут workflow_dispatch с `confirm_migrations_applied=true` (см. [[afk4-env-quirks]]).
- **B1-данные — DONE (ветка `feature/operator-map-data`, стек на #91):** floor-map DTO обогащён реальными `PlayerDisplayName`/`TariffName`/`SessionStartedAtUtc` (`EfFloorMapReadService` грузит PlayerAccounts+Tariffs словарями; гость/пакет→null). Accrued уже был реальным. Фронт: реальное имя вытесняет плейсхолдер, в панели блок «Игрок/Тариф/Начата HH:MM» (строки только когда бэк знает, #34), новый ключ `op.map.panel.startedLabel` (ru/en/tg), dev-mock обогащён. + footgun-фикс выше. Тесты зелёные (Platform.Api 1162; operator 221+88).
- **B1-геометрия — TODO: пространственная модель пола** (PosX/PosY/Rotation/SeatType, геометрия зон, WallEntity, EF-миграция). Нужна только под B2-редактор «План»; отложена до решения делать B2.
- **B2 — вид «План» вместо «Списка» (РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-06-16):** Таблицу/«Список» УБИРАЕМ, на её место — вид **«План»**: top-down планировка зала (НЕ изометрия — изо отложено как косметика; модель данных одна) с живыми статусами (видят все, право `floor_map.view`) + режим «Редактировать» под правом **`layout.manage`** (есть у branch_manager): палитра/инспектор/drag, сохранение одной транзакцией. Сетка остаётся абстрактной (координаты в неё НЕ тащим) — дефолт+фолбэк. Рендер: SVG-подложка (сетка/зоны/стены) + DOM-плитки (подход A). Спека `docs/superpowers/specs/2026-06-16-operator-map-plan-editor-design.md`. Режем на 3 PR: B2-1 бэкенд → B2-2 вид-просмотр → B2-3 редактор.
  - **B2-1 бэкенд — СМЕРЖЕН В main (PR #94, merge `1a43bc5`, 2026-06-16) + задеплоен на staging (health ok).** Миграция применена к staging вручную по рунбуку, порт закрыт обратно, push-run отменился concurrency, dispatch `confirm_migrations_applied=true` задеплоил success. План `docs/superpowers/plans/2026-06-16-operator-map-plan-b2-1-backend.md`. Сделано: `SeatEntity`+=PosX/PosY/Rotation/SeatType, `ZoneEntity`+=GeoX/Y/W/H/Color/ZoneType, новая `WallEntity` (Org+Branch скоуп); контракты (`SeatStatusDto`/`FloorMapDto`+`FloorMapWallDto`/bulk-request — всё хвостом с дефолтами, обр.совместимо); `FloorMapEtag.Compute` теперь 3-арг (zones,seats,walls) — геометрия+стены в хеше (иначе тихий clobber параллельных правок); read-сервис проецирует геометрию+стены; edit-сервис проставляет гео на create+update ветках + full-replace стен (tenant-фильтр Org+Branch); EF-миграция `20260616115144_AddFloorPlanGeometry` (аддитивна). `InternalsVisibleTo` добавлен в Api.csproj (тесты дёргают internal Etag). **1170/1170 Platform.Api тестов зелёные.** Minor follow-up (не блокер): нет explicit-валидации длины SeatType/ZoneType/Color и диапазона PosX/Rotation — следует паттерну `Name` (DB-лимит), можно поднять отдельной мелкой задачей.
  - **B2-2 (вид-просмотр «План», замена «Таблицы») — СМЕРЖЕН В main (PR #95, merge `23cc12a`, 2026-06-16).** План `docs/superpowers/plans/2026-06-16-operator-map-plan-b2-2-view.md`. Чисто фронт+i18n (миграций нет). Сделано: фронт-контракты получили геометрию; `OperatorFloorMapState` хранит `zones`/`walls`+гео мест (раньше выбрасывал); `floorPlanGeometry.ts` (`cellToPx`/`boundingBox`); `floorPlanState.toPlanModel`→`PlanModel`(placed/unplaced/zones/walls/bbox/isEmpty); `PlanSeat.tsx` (маркер, тон-язык плитки) + `FloorPlan.tsx` (SVG-подложка сетка/зоны/стены + DOM-слой мест, зум колесом/кнопками, пан с pointer-capture+pointercancel, native non-passive wheel); `MapWorkspace` переключатель `Карта·План`, Таблица удалена (разметка/CSS/i18n). i18n: удалены `op.map.col*`/`viewTable`/`tableLabel`, добавлены `op.map.plan.*`; **MapSidePanel переведён с `op.map.colPlayer/Device/Command` на новые `op.map.panel.{player,device,command}Label`** (те ключи были не только табличные — удаление в Task 7 ломало панель, починено). Решения: План ИГНОРИРУЕТ фильтр (фильтр=триаж Сетки); `rotation` хранится, но визуально НЕ применяется (доедет до редактора); стопка «не на плане» = только счётчик; пустой план→«Зал ещё не размечен» (координат нет до B2-3, Сетка полноценна). ⚠️ **tg-строки (plan + panel) — добросовестный черновик, ждут сверки носителем (не ru-копия).** Тесты: operator 238+88, i18n parity 22 — зелёные. Subagent-Driven (8 задач, двойное ревью).
  - **B2-3a (редактор «План», ЯДРО — расстановка мест) — СМЕРЖЕН В main (PR #96, merge `83a8bdb`, 2026-06-16).** План `docs/superpowers/plans/2026-06-16-operator-map-plan-b2-3a-editor-seats.md`. Чисто фронт+i18n (миграций нет; бэк PUT `/floor-map` + контракты уже в проде из B2-1). Сделано: `zoneId` на месте + `etag` карты в состоянии; API `getWithEtag`+`put(If-Match)`+`updateFloorMap`; `pxToCell`/`isCellOccupied`; `floorPlanDraft` (черновик + `toBulkUpdateRequest`); `FloorPlanEditor` (палитра «не на плане» · холст mode=edit · инспектор тип/поворот/убрать); drag со снапом+scale/origin; визуальный поворот; гейтинг `layout.manage`+живой ETag; «Редактировать»/«Разметить план»; подсказка не-менеджеру; `handleSaveLayout` (перезагрузка + маппинг 412/428/409); i18n `op.map.plan.edit.*`. **ГЛАВНЫЙ ИНВАРИАНТ:** PUT — full-replace, сериализатор шлёт ВСЕ места (incl. «не на плане») + зоны + стены, иначе сервер их удалит (покрыто тестом). Ревью (Opus) на полном диффе: APPROVE_WITH_NITS. Я делал Task 8 (интеграцию) сам — субагент на Task 6 сгаллюцинировал отчёт/отпочковался от устаревшей базы; Task 1 тихо сломал tsc-сборку (bun test не типчекает — гонять `bun run build`!). Остаток (некритично): нет живой подсветки занятой ячейки при drag; поворот крутит маркер с подписью (180°→имя вверх ногами).
  - **Тест-флак App.test — ПОЧИНЕН (PR #96, commit `650fd5f`).** `findByRole heading /AFK4 Dushanbe/` изредка таймаутил на дефолтных 1000ms при boot-е под нагрузкой CPU (изолированно 0/20, в файле редко). Корень — тайминг (не логика; не путать с задокументированной в `src/test/setup.ts` mock.module-утечкой между сиблингами, та закрыта сплитом App.test в отдельный bun-вызов в package.json). Фикс: `configure({ asyncUtilTimeout: 5000 })` в общий preload `src/test/setup.ts`. 50/50 зелёные.
  - **B2-3b (редактор зон) — СМЕРЖЕН В main (PR #97, merge `178e07b`, 2026-06-16).** План `docs/superpowers/plans/2026-06-16-operator-map-plan-b2-3b-zones.md`. Чисто фронт+i18n (миграций нет). Сделано: `DraftZone.serverId` (отделён от `zoneId`=client-key); мутации `addZone/setZoneGeometry/renameZone/setZoneColor/removeZone` + `setSeatZone` + `zoneSeatCount`; `toBulkUpdateRequest` шлёт `zoneId=serverId` (null→бэк создаёт зону), `clientId`=ключ, на который места ссылаются через `zoneClientId`; `FloorZonePanel` (список+«Добавить зону»), `FloorZoneInspector` (имя/X/Y/W/H числами с живым превью на canvas, цвет=пресет-свотчи, удалить), seat→zone дропдаун в `FloorInspector`, подсветка выбранной зоны на canvas; редактор: selection seat-XOR-zone, рейл палитра+панель-зон, новая зона = 4×3 под низом контента. **Гварды удаления (`disabled`+причина, не косметика):** зону с местами нельзя (осиротит → backend 400), последнюю зону нельзя (бэк требует ≥1). ВНЕ скоупа (отложено в B2-3c): стены, редактирование `zoneType` (мёртвые данные, нигде не рендерятся), drag-перенос зон. Subagent-Driven; Task 6 (интеграцию) и Task 7 (i18n) делал сам — субагент Task 3 вкрячил `(t as any)` касты (снял после добавления ключей). i18n parity 34 (таджикский реальный, allow-list не тронут). Независимое code-review полного диффа: блокеров нет. Тесты: operator 283+88, build OK. Некритичный долг: defensive `seat.zoneId ?? ''` в `createDraft` дал бы висячую ссылку, если появится создание мест без зоны с фронта (сейчас невозможно — бэк всегда шлёт GUID).
  - **B2-3c (стены) — СМЕРЖЕН В main (PR #98, merge `b2595a9`, 2026-06-16). ПОСЛЕДНИЙ хвост — эпик закрыт.** План `docs/superpowers/plans/2026-06-16-operator-map-plan-b2-3c-walls.md`. Чисто фронт+i18n. Сделано: драфт `addWall`/`removeWall(index)` (стены анонимны, full-replace, удаление по индексу = `draft-wall-${index}`); `FloorPlan` режим `tool='wall'` (клик по узлу→клик по второму→сегмент, снап к узлам, живая превью-линия + точка старта, нулевая стена отклоняется; клик по прозрачной hit-линии→`onDeleteWall`); редактор: тоггл «Стены» (чистит выбор + подсказка `wallHint`), wire `onAddWall`/`onDeleteWall`. `nodeFromEvent` переиспользует pan-agnostic формулу seat-drag. i18n `wallTool`/`wallHint` ru/en/tg (parity 34). Всё B2-3c делал САМ (хвост концентрирован в pointer-интеракции). Независимое ревью: материальных багов нет. Тесты: operator 289+88, build OK. Известный долг по B2-редактору (некритично): нет живой подсветки занятой ячейки при drag места; поворот крутит маркер с подписью (180°→имя вверх ногами); defensive `seat.zoneId ?? ''` в `createDraft` (висячая ссылка, если появится создание мест без зоны).

  - **B2-редактор ПЕРЕПИСАН на Konva (ветка `feature/operator-plan-konva-editor`, commit `cfa828f`, 2026-06-17 — НЕ смержено, ждёт визуальной проверки владельца).** Владелец: ручной SVG/DOM-канвас «очень сырой» — клик по месту сдвигал его, drag промахивался на клетку, кнопки сырые. Корень: самописная pointer-математика снапила угол места к клетке под курсором, игнорируя точку захвата и пан (#36 — взять правильный инструмент). Решение: канвас целиком на `react-konva` (Stage). `FloorPlan.tsx` переписан: места draggable+снап на отпускании (Konva держит точку захвата, click≠drag — порог 3px → клик только выделяет), зоны — ручки resize/move (`Transformer`), стены — линии, зум-к-курсору+пан из коробки; `getRelativePointerPosition` pan/zoom-aware → класс багов снапа исчез. Логика редактора вынесена в хук **`useFloorPlanEditor.ts`** (без Konva → тестируется `renderHook`, т.к. Konva не рендерится в happy-dom — это и причина, почему канвас-рендер-тесты удалены, корректность теперь в чистых функциях `floorPlanGeometry`: `snapPointToCell`/`snapZoneGeometry`). `FloorPlanEditor.tsx` теперь тонкая вьюха. **Цвета канваса** резолвятся из CSS-токенов в конкретный `rgb(a)` через DOM-пробу (`floorPlanTheme.ts`) — НЕ `color-mix` в canvas (парсер цвета `<canvas>` ему не всегда верит → риск невидимых зон). Чор­ма: тулбар-сегмент Выбор/Стены, primary «Сохранить»/ghost «Отмена», индикатор «не сохранено», плавающий зум, компактная акцент-кнопка «Редактировать» (была во всю ширину), читаемые подписи мест (без обрезки «P…»). Деп: `konva@10` + `react-konva@19.2.5` (peer react ^19.2). i18n +`toolLabel`/`toolSelect`/`unsaved` (parity 22, tg реальный). Тесты operator 278+88, build/tsc зелёные. **Чек самому невозможен (канвас визуальный, терминал браузер не видит)** — превью на `127.0.0.1:5174` (dev-mock обогащён непустым планом+ETag, dev-only). Известный долг B2-3a/b/c (поворот вверх-ногами, нет живой подсветки занятой ячейки) этим рефактором закрыт/снят. zoneType-редактирование по-прежнему вне скоупа.

**Консолидация состояний/тонов (commit `625f45f`) — было 7 тонов + двойной словарь, стало 5:**
- `SeatTone = ready|active|pending|blocking|offline` (убраны `warning`, `service`). Маппинг (`resolveTone`): paused→active; сессия+ПК-офлайн→**blocking** (красный «нужно действие», деньги капают без контроля — решение владельца); maintenance/service→offline (свёрнуто); failed→blocking.
- **Один словарь подписей**: `seatStatusLabel(tone)` в `floorMapState` (toneLabel делегирует туда). Плитка/панель/таблица показывают одно: Свободно/В сессии/Ожидание/**Внимание**/Нет связи (en Free/In session/Waiting/Attention/No link; tg Озод/Дар сессия/Интизорӣ/Огоҳӣ/Алоқа нест). [blocking-чип сначала был «Нужно действие», но обрезался в 86px-чипе → сокращён до «Внимание»; причина по-прежнему в строке-тела: «Сбой команды»/«ПК офлайн».] **Статус ПК on-demand (commit `2aa225f`):** статус ПК (пилюли связь/блокировка) СПРЯТАН по умолчанию, раскрывается ПРЯМО ПОД кнопкой «Статус» внутри «Управление ПК» (commit `0382ea9` — `pcStatusShown`, сброс при смене места); кнопка опрашивает ПК (спиннер→галочка), раскрывает пилюли, повторное нажатие — закрывает (тоггл, aria-expanded); отдельной секции «Статус ПК» сверху больше нет; сырой текст-отчёт убран (владелец: «сырое непонятно что»), ошибки остаются текстом. Строка «готовности» (нет прав/биллинг/команда в полёте) вынесена из блока статуса к действиям (`.panel-readiness`, всегда видна). Без устройства — нет «Управление ПК»/кнопки «Статус». `displayState` удалён, `stateLabel = seatStatusLabel(tone)`. Строка-тела плитки показывает ПРИЧИНУ (чип=состояние, тело=почему): blocking-без-сессии → «Сбой команды» (`remaining.action`), offline → «Нет связи с ПК», maintenance → «Закрыт».
- **Цвет = действие, кант=точка=ободок одним `--seat-color`** (убрана отдельная `--seat-ring`; раньше ободок ≠ кант): ready=accent, active=`--text-tertiary` (видимый нейтрал, был невидимый border-strong), pending=`color-mix(--warning 70%, --border-strong)` (приглушён), blocking=`color-mix(--danger 68%, --danger-strong)` (глубже, не томатный), offline=#64748b. Hover/focus/selected — по тону, не фирменный зелёный.
- `problemTones`/`isAttentionTone` = `{blocking, offline}` (pending=ожидание НЕ проблема). criticalAlertSources/фильтры обновлены (убран service-источник). tg-allowlist: убран `op.helper.tone.offline` (был «Офлайн»=tg, стал различаться).
- Осталось (некритично): мёртвые i18n-ключи `op.floor.state.*` (кроме free/locked), `op.helper.map.service`, `op.alerts.critical.service` — оставлены, безвредны.

Полировка панели/шапки (ветка `feature/operator-map-panel`):
- Нативные `<select>` → переиспользуемый `PanelSelect.tsx` (тёмный список, акцент+галочка на выбранном, клавиатура, клик-снаружи). Применён к 3 селектам панели (перенос/тариф/пакет). Причина: нативный popup ОС рендерится светлым и не несёт бренд. Тест `PanelSelect.test.tsx`.
- Из шапки убран тег режима сборки («· локальная сборка» рядом с именем оператора) — техшум; `shellModeLabel` остался экспортом, но в App.tsx больше не используется. Чип смены оставлен.
- Аккаунт оператора переехал из шапки (была текст-ссылка, не читалась как кнопка) в подвал рейла = `RailAccount.tsx`: аватар с инициалами → поповер-меню (имя + «Мой аккаунт» открывает AccountPanel + «Выйти»). Отдельная rail-кнопка «Выйти» свёрнута в это меню. Меню через `createPortal` в body (рейл с overflow:hidden иначе обрезал бы). Тест `RailAccount.test.tsx`. Имя виджета: 2 строки 11px/600 по центру, рейл получил больше bottom-padding (воздух).
- **Статус ПК унифицирован**: убран пункт «Статус» из ПКМ-меню (`seatMenu.ts` — нет `canStatus`/`pc-status`) и кнопка-тоггл «Статус» из секции «Управление ПК». Статус теперь постоянный блок «Статус ПК» ВНИЗУ карточки места (после «Управление ПК»): пилюли связь/блокировка + (при attention) версии/команда. Данные из floor-map seat — без отдельного опроса `/devices`+`/diagnostics` и без сырого toast. «Управление ПК» = только Блокировать/Разблокировать. `useFloorMap` всё ещё умеет action `status`, но из UI не вызывается.

Ключевые решения:
- **floor-map DTO тонкий**: имя игрока/тариф = плейсхолдеры (`t('op.floor.player.active')`), billing хардкод `Wallet`/`Fast guest` в `floorMapState.ts`. Настоящее наполнение = только через B1. Плитки наполняем тем, что реально знаем (остаток-герой, статус, тип проблемы).
- Контекст-меню (A2): исполняет только безопасные однотапные действия; денежные/критичные (завершить/оплата/пересадка/billed-старт) НЕ дублирует — правый клик выбирает место и раскрывает карточку справа, где подтверждение и quote. Гостевой биллинг — общий `guestBillingSelection` (operatorHelpers), чтобы меню и панель не разъехались. «Скоро»-пункты — честный toast (#37).
- Строка критсостояний (A2): через `ShellAlerts.sources`; `criticalAlertSources` показывает только тоны с реальным count (offline/blocking/service); клик → переход на карту + точечный фильтр. Фильтр карты поднят в App (MapWorkspace controlled); добавлены тон-фильтры `blocking`/`service` (не в ряду кнопок).

Связано: [[afk4-operator-app-webview2]] (Operator = WebView2+React, UI дёшев на Linux).
