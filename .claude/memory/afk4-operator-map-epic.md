---
name: afk4-operator-map-epic
description: Карта оператора = только ГРИД (вид «План» удалён); бэк-техдолг + durable-факты
metadata: 
  node_type: memory
  type: project
  originSessionId: fde4c0e8-7836-49c1-85e3-7b8af69217f9
---

Этап «Карта» ЗАКРЫТ и в main. **Разворот 2026-06-17: вид «План» удалён целиком** (`cc72a37`) — карта = только **грид**. Konva-редактор плана выброшен (старые заметки «B2 на Konva, ждёт владельца» неверны).

Durable:
- **Бэк-техдолг**: PUT `/floor-map` + `WallEntity` + гео-колонки места НЕ тронуты → висит мёртвый write-endpoint (фронт его не зовёт). Сохранены GET-слой/transport/read-DTO/грид.
- **Типографика оператора**: веса 500/600/700; 800 только числа-герои.
- **Терминология копи**: «Платформа»→«сервер» в операторском тексте.
- **NB не путать**: `floor.*` i18n и Platform.Web `FloorMapEditor` — это ДРУГОЙ (venue) редактор, живой.

**Полировка карты 2026-06-19 (в работе/main):**
- Удалён мигающий плейсхолдер: `createFixtureFloorMapState()` теперь с пустыми `seats` (был статический массив 24 фейк-ПК с играми) → в окне загрузки скелетон. Статический `seats` из `operatorData.ts` выпилен; `seats[0].id`-фолбэки → `''`.
- На плитке справа от имени ПК — имя клиента (`playerDisplayName` или «Гость» для сессии без аккаунта); свободное/проблемное место без сессии — без строки. `.seat-client` в `06-map-grid.css`.
- Шкала остатка времени — РЕАЛЬНАЯ доля: `остаток / (прошло + остаток)`, прошло = now−`sessionStartedAtUtc`; фолбэк на «остаток от часа» (`SEAT_TIME_BAR_CEILING_SECONDS`) только без старта сессии. В `seatTilePresentation.ts`.
- Поле `SeatSummary.billing` удалено целиком (было мёртвое — нигде не отображалось). НЕ путать с `SessionBillingSelection` (выбор тарифа при старте) и `op.helper.billing.*` — это живое.
- Строка «Команда» в правой панели (`commandLabel(seat.command)`) — теперь только для `tone === 'pending' | 'offline'` (для active/ready/service дублировала чип).

**БЭКЛОГ — «Активное окно» (что за игра/приложение у клиента):** floor-map DTO этих данных НЕ несёт; получить можно только после работы с **клиентским шеллом** (шелл должен слать активное окно). Пока — честный пункт со статусом «СКОРО» в меню первого клика по ПК (`seatMenu.ts`, секция soon, id `soon-active-window`, ключи `op.map.menu.activeWindow*`). Когда шелл научится — заменить стаб на живые данные (план: показывать над «осталось» на плитке + в правой панели).
