---
name: platform-product-operations-wave-d
description: "Волна D эпика «пробелы платформенной админки» — продуктовые операции; спека на пять планов, план 1 (лимиты тарифа) в main."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5cc09261-f4ea-4bf1-a26f-2ca49d85c51e
  modified: 2026-08-09T21:09:52.490Z
---

Последняя волна эпика (A доступ, B деньги, C наблюдение — закрыты; см. [[platform-admin-directory-2fa]],
[[platform-billing-dunning-wave-b]], [[platform-observability-wave-c]]).

Спека `docs/superpowers/specs/2026-08-08-platform-product-operations-design.md` — четыре раздела,
**пять планов** (§1 разрезан надвое):

1. **Лимиты тарифа** — `docs/superpowers/plans/2026-08-08-platform-plan-limits.md`, **PR #143 в main
   `a04c539c`**. Закрыт.
2. **Фичи** — `docs/superpowers/plans/2026-08-09-platform-feature-entitlements.md`, **PR #144 в main
   `4379eaf2`**. Закрыт.
3. **Роли платформы в базу** — `docs/superpowers/plans/2026-08-09-platform-roles-in-database.md`,
   **закрыт целиком**: задачи 1-2 в `83373106` (PR #145), задачи 3-5 в `8a3e4943` (PR #146).
4. **Анонсы платформы** — `docs/superpowers/plans/2026-08-10-platform-announcements.md`,
   **PR #148 в main `74aa8a0c`**. Закрыт.
5. **Офбординг клуба** — срок, выгрузка ZIP/CSV, стирание личного с сохранением финансового.
   НЕ начат.

## Решения волны (durable)

- **Лестница разрешений одна:** ручное исключение для клуба → тариф → умолчание фичи. В панели
  видно, **чем именно решено** («не куплено» и «не выкачено» — разные ответы клиенту).
- **Каталоги объявлены в коде, живут в базе, редактируются в панели** (как
  `BillingPlanSeedHostedService`). Только-база порождает флаги-призраки и молча выключенные фичи,
  которых не ловит ни один тест.
- **Связка «право» vs «роль»:** названия прав обязаны быть в коде (эндпоинты на них ссылаются);
  в данные переезжают роли. Предохранители: нельзя выдать больше, чем есть у тебя;
  `ManagePlatformAdmins` нельзя добавить/отнять себе; последний полный админ неприкосновенен;
  права считаются по текущему составу роли, а не по слепку на момент входа.
- **Анонс:** отметка прочтения на **сотрудника**, не на клуб (иначе первый закрывший полосу
  скроет её у всех). Письмо ровно один раз при публикации. Текст — одно поле, машинного перевода
  на en/tg нет осознанно.
- **Офбординг:** авто-стирания по таймеру нет, срок только показывает «пора». Стираем личное,
  оставляем финансовое; финальный статус `purged` отдельно от `deletion_pending`. Тест обязан
  проверять **обе стороны** — и что личное исчезло, и что финансовое уцелело.

## Durable-уроки плана 1

- **Лимиты были чистой полу-имплементацией:** `LimitsJson` + экран в панели + ноль читателей.
  Теперь единственные точки решения — `OrganizationLimitsJson` (разбор) и `IPlanLimitGuard`
  (решение), четыре точки роста зовут их.
- **«Стоп на рост, старое живёт»:** `current >= limit` перед добавлением, а не «превышено ли сейчас».
- **Второго филиала в продукте не существовало** — ни кнопки, ни эндпоинта, филиал появлялся только
  вместе с организацией. Лимит филиалов проверял бы невозможное, поэтому в план добавлен
  `POST /api/platform/organizations/{id}/branches`. Мёртвая проверка = ложно-зелёное «сделано».
- **Правило должно держаться на ВСЁМ пути.** Дважды за план числа отказа терялись при мэппинге:
  путь старта сеанса из брони (`ReservationSessionStartResult`) и — осознанно отложено — эндпоинт
  привязки устройства. Проверять надо каждый путь до тела HTTP-ответа, а не первый попавшийся.
- **Двойной счёт на границе:** приём приглашения считал само принимаемое приглашение как занятое
  место → при лимите N последнее приглашение принять было нельзя. Лечится
  `CheckStaffUserAsync(..., excludingInviteId)`. Граничных тестов «ровно N» не было ни у кого —
  ошибку поднял сам исполнитель как сомнение.
- **Ложное объяснение отказа хуже отказа:** занятый slug проверяется ДО лимита; неизвестная
  организация не отказывается «по лимиту» (guard возвращает `null`, за «нет такой» отвечает
  вызывающий).
- **Отказ несёт числа:** код `plan_limit_reached` + предел/текущее/тариф; фраза собирается клиентом.
  В Операторе точка расширения — `projectOperatorError` в `src/apiErrors.ts` (таблица код→ключ);
  для чисел ей понадобился проброс значений в `t()`.

## Durable-уроки плана 2 (фичи)

- **Каталог фич:** объявление в коде (`FeatureCatalog.Declared`) → сидер заводит недостающие ключи,
  никогда не переписывает существующую строку (после создания авторитетна панель) и не выходит рано
  на непустом каталоге. Четыре ключа: `online_booking`, `loyalty`, `online_topup`, `player_shop`.
- **`plan_features` стартует ПУСТОЙ, умолчание каждой фичи = включена** — поведение продукта не
  изменилось ни на йоту. Придумать раскладку «что входит в какой тариф» = молча поменять продукт
  под видом инфраструктуры; это коммерческое решение, которого никто не принимал.
- **Лестница живёт в `FeatureLadder.Resolve`** — чистая функция, которой пользуются ОБА пути:
  быстрый `IsEnabledAsync` (один запрос с коррелированными подзапросами, для горячих путей) и
  полный `DescribeAsync`. Тест согласованности обоих путей на всех сочетаниях ступеней — страж.
- **Молчание значит разное в разных подсистемах:** у лимитов несуществующая организация = «не
  отказывать», у фич = «выключено». Не переносить интуицию с одного на другое.
- **Гейт фичи ставится и на побочном эффекте, а не только на чтении:** лояльность гейтится в
  `LoyaltyAccrualService.BuildCashbackEntryAsync`, иначе выключенная фича продолжала бы начислять
  бонусы, а экран бы просто исчез.
- **Платёж в полёте зачисляется даже после выключения фичи** (эскхата-статус) — намеренно, решение
  пользователя, зафиксировано тестом + комментарием. Закрыть = списать деньги с карты и не
  зачислить. Ревьюер назвал это Critical; правильным ответом было НЕ чинить.
- **Fail-open в интерфейсе:** список фич не пришёл → считаем включённым (сервер всё равно откажет).
  Но **некорректная форма 200-ответа обязана уводиться в тот же путь**: `Array.isArray`-проверка в
  клиенте, иначе `undefined` едет вниз и `.includes()` в глобальном компоненте роняет весь экран в
  белое (ErrorBoundary в клиентских приложениях нет).
- **Скрывать надо все входы, а не пункт меню:** в оболочке `ShopScreen.onNeedTopUp` открывал экран
  пополнения в обход спрятанной кнопки.
- **Рычаги прячутся по настоящему праву, а не по имени роли:** вкладка «Фичи» видна поддержке
  (читать можно), органы управления — только при `platform.organizations.features.manage`.

## Durable-уроки плана 3 (роли, задачи 1-2)

- **Права считаются из базы на КАЖДОМ запросе** — контекст админа и так строился per-request
  (`PlatformAdminAuthenticationMiddleware` → `ValidateAsync` → `CreateContextAsync`), поэтому снятое
  право отваливается сразу. **Кэш здесь запрещён** — он отменяет смысл переезда.
- **Флаг `GrantsAllPermissions` у роли** вместо перечисления прав администратора: без него новое
  право после деплоя не принадлежит никому и раздел недоступен всем до ручной правки роли.
  Сверено лично: старый набор admin = ровно все 23 права из кода, эквивалентность точная.
- **Право, исчезнувшее из кода, отсекается при резолве** (`IntersectWith(All)`), иначе старая строка
  в базе продолжала бы действовать.
- **`PlatformAdminPermissionNames.All` + reflection-страж** на полноту: право, забытое в списке,
  нельзя ни выдать роли, ни увидеть в панели.
- **Порядок хостед-сервисов значим:** сидер ролей обязан стартовать раньше бутстрапа первого
  администратора.
- **Тест может начать проходить ПО СОВПАДЕНИЮ:** тесты бутстрапа перестали различать «известная
  роль принята» и «фолбэк на умолчание», потому что их контейнер не сеял роли, а умолчание
  совпадало с ожиданием. Все ассерты зелёные, покрытие нулевое.

## Durable-уроки плана 3 (задачи 3-5, сделаны контроллером без подагентов)

- **Самопонижение = сравнение НАБОРОВ прав, не их количества.** Две роли по десять прав могут не
  пересекаться вовсе; по счёту переход выглядит равноценным, отбирая при этом всё.
- **«Полный админ» = флаг `GrantsAllPermissions` у роли**, а не имя `platform_admin`.
  **Но:** при пустой таблице ролей предохранитель молча испарялся — Postgres-тест гонки показал,
  что отключились ОБА администратора. Правило: в сломанном состоянии предохранитель становится
  строже (фолбэк на встроенное имя), а не исчезает. Отдельный тест на пустую таблицу обязателен.
- **Предохранитель «последняя роль с ключом» недостижим и удалён:** чтобы дойти до эндпоинта, надо
  самому иметь `platform.admins.manage`, значит своя роль его даёт и целевая не последняя. Написал →
  тест показал недостижимость → убрал ветку, а не подогнал сцену. Проверка, которая не может
  сработать, — ложное чувство защиты.
- **Состав роли с полным доступом не редактируется** (флаг, а не список: иначе фиксируется
  сегодняшний набор и теряются завтрашние права).
- **Гонка за именем роли:** read-then-write + PK; проигравший обязан получить `role_name_taken`
  либо `conflict`, но не 500. Ловится только настоящим Postgres.
- **`PlatformApiError.errorCode` в панели берётся из поля `error`**, а не `code` — эндпоинты
  отдают код в обоих полях.
- **`ErrorState` в PlatformControl.Web принимает `message`, не `title`**; `bun test` этого не
  ловит, ловит только `bun run build`.

## Что нашло независимое ревью плана 3 (PR #147 в main `1ae5a6e4`)

Ревьюера у #145/#146 не было; разбор постфактум дал шесть находок, одна с дырой в предохранителе.

- **Неполная нормализация = дыра.** Права дедуплицировались ignore-case, но сохранялись в
  присланном написании, а два сравнения в правке роли были посимвольными. Итог:
  `PLATFORM.ADMINS.MANAGE` действовало как право, но при следующем сохранении роли молча
  удалялось — мимо проверки самоблокировки. **Правило: если сравниваешь без учёта регистра —
  нормализуй ХРАНИМОЕ значение, иначе живут две «одинаковые» строки.**
- **Отказ по существу — тоже событие аудита.** Писались только `Denied` по правам и успех; попытка
  выдать себе право, которого нет (`permission_not_held_by_actor`), следа не оставляла.
- **Имя из URL нормализуется там же, где при создании** — иначе `PUT /roles/CLERK` не находит
  `clerk` (Postgres регистрозависим).
- **Пропавшие паттерны на фронте:** удаление роли без `ConfirmDialog`, который стоит в соседней
  секции ТОГО ЖЕ экрана; права выведены машинными ключами при живом `@afk4/i18n`
  (23 права + 7 разделов переведены в #147, ключи `platform.permission.*`).
- Новые бэкенд-тесты проверены **на откате фикса** — без него падают три из трёх.

## Durable-уроки плана 4 (анонсы)

- **Сторож рассылки — отметка на сущности, а не «статус только что сменился».**
  `EmailDispatchedAtUtc` ставится один раз: снять и опубликовать заново = то же сообщение.
  Второй слой — ключ идемпотентности очереди `announcement:{id}:{orgId}`.
- **Postgres-тест гонки нашёл дефект, которого не видел ни один InMemory-тест:** два
  одновременных «опубликовать» доходят до постановки писем оба, проигравший упирался в уникальный
  индекс очереди → сырой 500. Лечится тем же приёмом, что у ролей: unique-violation в
  serializable-обёртке = `conflict` («повторите»).
- **Фейк в тесте должен мерить ТО ЖЕ, что видит пользователь.** Первая версия считала вызовы
  `INotificationService` и «нашла» дубль там, где его нет: настоящий `EfNotificationOutbox` пишет
  в ТОТ ЖЕ `PlatformDbContext`, и вставка проигравшего уходит с его откаченной транзакцией.
  Считать надо строки в базе после гонки.
- **Аудитория считается при выдаче, получатели письма — на момент публикации.** Полоса должна
  дойти до клуба, заведённого завтра; рассылать письма вечно новым клубам — рассылка без конца.
- **Правка опубликованного: текст можно, важность и аудиторию нельзя** — письма ушли по прежнему
  списку, смена задним числом делает след аудита неправдой.
- **«Новости» в Операторе уже заняты** (`NewsWorkspace`, `/api/news`) — это новости КЛУБА его
  игрокам, противоположное направление. Анонсы названы «Сообщения платформы»; спутать = показать
  оператору чужой экран под знакомым именем.
- **Слот полосы в шелле один** (`--shell-banner-h`, 40px): режим поддержки → задолженность →
  анонс. Поэтому постоянный вход в историю — кнопка со счётчиком в `ShellHeader`, а не полоса.
  Отдельная зона рейла не годится: разделы гейтятся правами настройки, а сообщение адресовано всем.
- **Защита формы ответа нужна на КАЖДОЙ границе, а не только в клиенте:** хук принимает любой
  клиент, и `Array.isArray` в одном лишь `createPlatformMessagesClient` не спас бы шелл от белого
  экрана. Тест это показал.
- Тариф клуба читается из `Organizations.PlanCode` (как у лимитов и фич), не из
  `OrganizationSubscriptions`.

## Открытые хвосты

- Отложенные миноры плана 1: эндпоинт привязки устройства не отдаёт числа наружу; мёртвый
  `InMemoryDeviceEnrollmentService` без проверки лимита; гонка по slug без маппинга
  `DbUpdateException` в 409 (тот же пробел у создания организации — чинить одним патчем);
  generic-тост поверх конкретной причины в `NewBranchDialog`; опечатка «едят» в комментарии
  `PlanLimitExceededDto`.
- **Флак Оператора, не связанный с веткой:** `App under an active support session > renders the real
  operator shell` падает в полном прогоне и на `main`, в одиночку проходит — протечка между тестами
  (см. [[frontends-on-bun-test]] про `mock.module`).
- **Пропущенный тест ничего не доказывает** — на этом прогорели: `ShopCommercePostgresFixture`
  собирает контейнер вручную и не знал о новой зависимости `LoyaltyAccrualService`, падало
  построение сервиса; видно стало только при заданном `AFK4_COMMERCE_TEST_POSTGRES`. Проверять
  ветку надо с ВСЕМИ строками подключения и `AFK4_REQUIRE_POSTGRES_TESTS=1`.
- Прогон бэкенда с настоящим Postgres требует **четырёх** переменных, не одной:
  `AFK4_PLATFORM_ADMIN_…`, `AFK4_RESERVATION_…`, `AFK4_POS_POSTGRES_TEST_CONNECTION_STRING`,
  `AFK4_COMMERCE_TEST_POSTGRES` (+ `AFK4_REQUIRE_POSTGRES_TESTS=1`). С одной остаются skip и одно
  падение.
- Ветка плана делалась от `main`, но **коммиты спеки и плана не были запушены в origin** — они
  уехали внутрь squash-мержа, и локальный `main` разошёлся с удалённым. Лечится
  `git reset --hard origin/main` после проверки, что документы совпадают.
