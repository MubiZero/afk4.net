/** Re-export of the shared `@afk4/money` workspace package so the whole app keeps
 * a single rounding convention. The implementation lives in `packages/money`. */
export { minorToMajor, majorToMinor } from '@afk4/money';
