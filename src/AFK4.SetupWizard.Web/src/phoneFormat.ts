/** Ре-экспорт общего кода из `@afk4/formatting`. Здесь лежала копия того же файла из админки
 * клуба, отличавшаяся только языком комментариев. */
export { localPhoneDigits, formatLocal, fullPhoneDigits } from '@afk4/formatting';
